---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: ocisecuritypolicies.compute.nvcne-demo.io
spec:
  group: compute.nvcne-demo.io
  names:
    kind: OCISecurityPolicy
    listKind: OCISecurityPolicyList
    plural: ocisecuritypolicies
    singular: ocisecuritypolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.securityListId
      name: SecurityListID
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OCISecurityPolicy is the Schema for the ocisecuritypolicies API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OCISecurityPolicySpec defines the desired state of OCISecurityPolicy
            properties:
              compartmentId:
                description: CompartmentID is the OCID of the compartment containing
                  the security list.
                type: string
              displayName:
                description: DisplayName is the human-readable name for the security
                  list.
                maxLength: 255
                minLength: 1
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: FreeformTags are key-value pairs you can attach to the
                  security list.
                type: object
              rules:
                description: Rules is the list of security rules to apply.
                items:
                  description: SecurityRule defines a single ingress or egress rule
                  properties:
                    description:
                      description: Description is a human-readable description of
                        the rule.
                      type: string
                    destination:
                      description: Destination is the destination CIDR for egress
                        rules (e.g. "0.0.0.0/0").
                      type: string
                    direction:
                      description: Direction specifies whether this is an INGRESS
                        or EGRESS rule.
                      enum:
                      - INGRESS
                      - EGRESS
                      type: string
                    maxPort:
                      description: MaxPort is the maximum port number for the rule.
                      format: int32
                      maximum: 65535
                      minimum: 1
                      type: integer
                    minPort:
                      description: MinPort is the minimum port number for the rule.
                      format: int32
                      maximum: 65535
                      minimum: 1
                      type: integer
                    protocol:
                      description: Protocol is the IP protocol (e.g. "6" for TCP,
                        "17" for UDP, "all" for all protocols).
                      type: string
                    source:
                      description: Source is the source CIDR for ingress rules (e.g.
                        "0.0.0.0/0").
                      type: string
                  required:
                  - direction
                  - protocol
                  type: object
                minItems: 1
                type: array
              vcnId:
                description: VcnID is the OCID of the VCN that owns the security list.
                type: string
            required:
            - compartmentId
            - displayName
            - rules
            - vcnId
            type: object
          status:
            description: OCISecurityPolicyStatus defines the observed state of OCISecurityPolicy
            properties:
              conditions:
                description: Conditions represent the current state of the OCISecurityPolicy
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              failureReason:
                description: FailureReason contains the error message if the policy
                  failed.
                type: string
              observedGeneration:
                description: ObservedGeneration is the last generation reconciled
                  by the controller.
                format: int64
                type: integer
              phase:
                description: Phase represents the current lifecycle phase of the security
                  policy.
                enum:
                - Pending
                - Creating
                - Active
                - Updating
                - Deleting
                - Deleted
                - Failed
                type: string
              securityListId:
                description: SecurityListID is the OCID of the provisioned OCI security
                  list.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
